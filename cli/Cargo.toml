[package]
name = "cs-cli"
version = "3.0.1"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tokio = { version = "1.6", features = ["rt", "macros"] }
serde_json = "1.0"
chainseeker = { path = "../client" }
